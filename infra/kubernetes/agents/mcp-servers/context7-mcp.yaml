apiVersion: apps/v1
kind: Deployment
metadata:
  name: context7-mcp
  namespace: dev-team-agents
  labels:
    app: context7-mcp
    team: dev-team
    component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: context7-mcp
  template:
    metadata:
      labels:
        app: context7-mcp
        team: dev-team
        component: mcp-server
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "context7-mcp-role"
        vault.hashicorp.com/agent-inject-secret-token: "secret/data/dev-team/mcp/context7"
    spec:
      securityContext:
        runAsUser: 1001
        runAsNonRoot: true
        fsGroup: 0
      containers:
        - name: context7-mcp
          image: registry.redhat.io/rh-ee-mcp/context7-mcp-server:latest
          ports:
            - containerPort: 3000
              name: mcp
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          envFrom:
            - secretRef:
                name: context7-mcp-credentials
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: context7-mcp
  namespace: dev-team-agents
  labels:
    app: context7-mcp
spec:
  selector:
    app: context7-mcp
  ports:
    - name: mcp
      port: 3000
      targetPort: 3000
